// LAYER: Configuration — Production Environment
// PURPOSE: API URL for the deployed version on AWS.
//          Replace with your actual CloudFront/EC2 URL when deploying.
export const environment = {
  production: true,
  apiUrl: 'https://api.yourdomain.com',  // Replace with actual backend URL
};
